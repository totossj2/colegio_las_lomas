# Configuración mínima para evitar errores 403
DirectoryIndex index.html index.htm

# Redirección forzada a HTTPS (solo si el módulo está disponible)
# DESCOMENTAR ESTAS LÍNEAS DESPUÉS DE INSTALAR EL CERTIFICADO SSL
# <IfModule mod_rewrite.c>
#     RewriteEngine On
#     RewriteCond %{HTTPS} off
#     RewriteCond %{HTTP_HOST} ^(www\.)?colegiolaslomas\.com$ [NC]
#     RewriteRule ^(.*)$ https://www.colegiolaslomas.com/$1 [R=301,L]
# </IfModule>

# Headers de seguridad
<IfModule mod_headers.c>
    # Forzar HTTPS - DESCOMENTAR DESPUÉS DE INSTALAR SSL
    # Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
    # Prevenir clickjacking
    Header always set X-Frame-Options "SAMEORIGIN"
    # Prevenir MIME type sniffing
    Header always set X-Content-Type-Options "nosniff"
    # XSS Protection
    Header always set X-XSS-Protection "1; mode=block"
    # Referrer Policy
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
</IfModule>

# Habilitar compresión GZIP (solo si el módulo está disponible)
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/css text/javascript application/javascript application/json
</IfModule>

# Cacheo básico (solo si el módulo está disponible)
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType image/webp "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/svg+xml "access plus 1 year"
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
</IfModule>
